name: pointnet++
verb_embedding_dim: 64
sa1_n: 512     # SA level-1 output points
sa2_n: 128     # SA level-2 output points
k: 16          # k-NN neighbors per SA layer
# MLP-head fields (not used by pointnet++, present to avoid Hydra missing-key errors)
layer_dims: []
dropout: 0.0
