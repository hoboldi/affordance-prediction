# @package _global_
defaults:
  - override /data: default
  - override /training: default

hydra:
  run:
    dir: outputs/overfitting/nine

data:
  subset: nine
  batch_size: 3
  num_workers: 0

training:
  overfit: true
  epochs: 200

model:
  dropout: 0.0
