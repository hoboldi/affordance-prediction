# @package _global_
defaults:
  - override /data: default
  - override /training: default

hydra:
  run:
    dir: outputs/overfitting/ninety

data:
  subset: ninety
  batch_size: 8
  num_workers: 4

training:
  overfit: true
  epochs: 200

model:
  dropout: 0.0
