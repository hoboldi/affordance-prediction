# @package _global_
defaults:
  - override /data: default
  - override /training: default

hydra:
  run:
    dir: outputs/overfitting/single

data:
  subset: single
  batch_size: 1
  num_workers: 0

training:
  overfit: true
  epochs: 200
  lr_schedule: cosine
  lr: 1e-3

model:
  dropout: 0.0