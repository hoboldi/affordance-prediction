# Install as a matched pair BEFORE requirements.txt / pip install -e .
# Do not upgrade torch and torchvision independently.
#
#   pip install -r requirements-pytorch.txt
#
# Fresh env (recommended if you see torchvision::nms or libjpeg errors):
#   pip uninstall -y torch torchvision torchaudio
#   pip install -r requirements-pytorch.txt

# HOST environment (this file is not used inside the SAM3D Docker image, which ships its own
# torch 2.5.1+cu121; pyproject only sets a >=2.5.1 floor so `pip install -e .` there won't upgrade it).
torch>=2.6
torchvision>=0.21
