# 1) PyTorch first (matched pair):  pip install -r requirements-pytorch.txt
# 2) Then the rest:                 pip install -r requirements.txt
# Or in one go after PyTorch:       pip install -e ".[notebooks,dev]"

numpy>=1.24
Pillow>=10.0
PyYAML>=6.0
trimesh>=4.0
pyrender>=0.1.45
PyOpenGL>=3.1.0
tqdm>=4.65
loguru>=0.7
transformers>=4.36
safetensors>=0.4

# GEAL pseudolabel pipeline + training metrics
scipy>=1.10           # nearest-point mapping of GEAL scores -> mesh vertices
pandas>=2.0           # GEAL Affordance-Question.csv lookup (labeling.geal_infer)
scikit-learn>=1.3     # train_affordance.py validation metrics (AUPRC)
huggingface_hub>=0.20 # fetch GEAL / SAM3D checkpoints (scripts/bootstrap.sh)

# torch + torchvision are in requirements-pytorch.txt (install separately)

# SAM3D (optional): git submodule update --init sam-3d-objects
# GEAL teacher code + its own deps: scripts/bootstrap.sh, then pip install -r external/geal/requirements.txt
