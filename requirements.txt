numpy>=1.24
torch>=2.0
Pillow>=10.0
PyYAML>=6.0
trimesh>=4.0
tqdm>=4.65
loguru>=0.7

# SAM3D (install submodule separately: git submodule update --init)
# See sam-3d-objects for additional CUDA / model dependencies.
